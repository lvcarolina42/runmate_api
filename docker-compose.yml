services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: runmate
      POSTGRES_USER: runmate
      POSTGRES_PASSWORD: runmate
    ports:
      - "5432:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
